version: "3.9"

services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    environment:
      GROQ_API_KEY: ${GROQ_API_KEY}
      MONGODB_URI: ${MONGODB_URI}
      DB_NAME: voiceagent
    restart: unless-stopped

